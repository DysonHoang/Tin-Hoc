#include <bits/stdc++.h>

using namespace std;

int main()
{string st;
char st;
    getline(cin,st);
    for(int i=0;i<=st.size()-1;i++)
    {
        s[i]-=32;
    }
for(int i=0;i<=st.size()-1;i++)
    cout << st[i];
    return 0;
}
